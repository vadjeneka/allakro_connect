<%= form_with model: [store, product, bid, bid.offers.build] do |form|%>
  <%= form.number_field :amount, placeholder: "Votre offre: 50000 XOF", class:"border-2 rounded-lg p-2 w-3/4"%>
  <%= form.submit %>
<% end %>