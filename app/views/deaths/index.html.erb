<style>
  .min-h-s{
    min-height: 10rem;
  }
</style>

<div class="p-5 pl-10">
  <h2 class="text-3xl font-bold my-7 ml-5">Liste des décès d'Allakro </h2>
  <hr>
  <div class="mt-5 w-full flex ">
    <div class="flex-grow"></div>
      <%= link_to 'Ajouter un décès', new_death_path, class:'bg-green-600 text-white px-5 py-2 font-medium rounded' %>
  </div>
  <div class="mt-5">
    <% if @deaths %>
      <%= render partial: "shared/death_table", locals: { deaths: @deaths, user: @user } %>
    <% end %>
  </div>
</div>

<% content_for :banners do %>
  <%=render 'shared/banners'%>
<% end %>

<% content_for :footer do %>
  <%=render 'shared/footer'%>
<% end %>