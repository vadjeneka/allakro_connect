<style>
  .min-h-s{
    min-height: 10rem;
  }
</style>

<div class="p-5 pl-10">
  <h2 class="text-3xl font-bold my-7 ml-5">Recensement des actualités d'Allakro </h2>
  <hr>
  <div class="max-w-screen-xl mx-auto p-5 sm:p-10 md:p-16">
    <div class="grid grid-cols-1 md:grid-cols-2 sm:grid-cols-2 gap-10">
      <!-- birth card -->
      <%=render 'births/birth'%>
      <!-- death card -->
      <%=render 'births/death'%>
      <!-- job card -->
      <%=render 'births/job'%>
      <!-- Aménagement card -->
      <%=render 'births/location'%>

      <%=render 'births/move'%>
    </div>
  </div>
</div>

<% content_for :banners do %>
  <%=render 'shared/banners'%>
<% end %>

<% content_for :footer do %>
  <%=render 'shared/footer'%>
<% end %>